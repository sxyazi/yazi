[package]
name    = "config"
version = "0.1.0"
edition = "2021"

[dependencies]

shared = { path = "../shared" }

# External dependencies
toml      = { version = "^0", features = [ "preserve_order" ] }
xdg       = "^2"
serde     = { version = "^1", features = [ "derive" ] }
once_cell = "^1"
crossterm = { version = "^0" }
anyhow    = "^1"
ratatui   = "^0"
glob      = "^0"
futures   = "^0"
